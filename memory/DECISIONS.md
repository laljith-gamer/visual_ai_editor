# DECISIONS

> A running log of important **technical and product decisions**, with the
> reasoning and impact. The goal: future sessions understand *why* the
> project is the way it is, and don't accidentally undo intentional choices.
>
> Add a new row at the **top** of the table (newest first). Keep entries
> short and factual. If a decision is later reversed, add a new row that
> supersedes it (don't delete history).

## How to add an entry

- **Date:** ISO format `YYYY-MM-DD`.
- **Decision:** what was decided, in one line.
- **Reason:** the main driver.
- **Impact:** what this affects (files, behavior, future work).

## Decision log

| Date | Decision | Reason | Impact |
|------|----------|--------|--------|
| 2026-06-18 | **Control commands (yes/ok/no/cancel/undo/redo/render/export) are classified by a pure ANCHORED regex (`lib/intent/fastCommands.ts`) and resolved BEFORE the planner; they never become content searches.** Anchored `^…$` matching keeps partial commands ("go to clip 2", "render the part where he scores") flowing to the parser. | The reported bug: "yes do it"/"undo" fell through to the planner and became "look for yes/undo moments". A tiny deterministic gate fixes it with <1s latency and no WebGPU. | New `fastCommands.ts`; `runAgentCommand` fast path with strict priority; affirm/cancel WITH a pending action delegate to the existing quick-shortcut gate (no duplicated logic); one-step `redoTimeline` added to the store; "render" wired to the real `handleRender`. |
| 2026-06-18 | **Agent memory persists to its own IndexedDB store (`shorts-studio-agent-memory`); only the compact serialization (records/flow/reinforcement) is stored — never video blobs/transcript text/frames.** Retrieval prioritises user_stated > reinforcement > clip > source > flow > observed > preference. | Offline-first: useful memory must survive refresh without bloating storage. Direct user instruction must outrank inferences. | `idb` `agentMemory` kind + `lib/agent-memory/persistence.ts` + pure `getRelevantMemory`; runner hydrates once/session and saves after memory-mutating turns. |
| 2026-06-18 | **Local caches are budgeted (mobile 150/50/100 MB, desktop 600/300/500 MB; warn before downloading a model >80 MB) with measurement + explicit cleanup.** | The site could silently grow to GBs; offline-first needs measurable, controllable storage and a permission prompt before large model downloads. | `STORAGE_BUDGET` in config + `lib/storage/budget.ts` (pure, tested) + `lib/storage/manager.ts` (Cache Storage/idb/`navigator.storage` measurement + cleanup). Panel UI still TODO. |
| 2026-06-18 | **Transcription failures surface an explicit error ("Transcription failed: <reason>") instead of silently showing "No transcript yet".** | The hook's catch left progress unchanged, so the drawer fell back to the empty state with no reason — the user's reported problem. | `useTranscription` sets `phase:"error"`; `TranscriptDrawer` renders an error card with Try again. |
| 2026-06-17 | **Add a deterministic agentic intent layer that resolves natural editing commands into structured timeline operations, running BEFORE the cloud planner; keep the LLM as a fallback/enhancement, not the only resolver.** Source/clip/range/concept/placement references + append-vs-replace-vs-move-vs-remove + reinforcement are parsed deterministically; concept search uses the LOCAL transcript first, OCR reports honestly unavailable, and generic "best parts" defers to the existing visual pipeline. | The editor behaved like a command bot past the narrow `quickMatch` patterns. Deterministic-first parsing is instant, testable, and avoids a cloud round-trip; the project rule is "use deterministic parsing first, LLM as fallback". | New `lib/intent/*` (command parsing), `lib/agent-memory/*` (confidence+evidence memory + reinforcement + policy), `lib/timeline/*` (pure ops), `lib/agent/*` (orchestrator/conceptResolver/reinforcement/runAgentCommand), `lib/ocr/*` (honest stub). Wired into `handleAgent` before `tryQuickShortcut` (additive/reversible). `AGENT_POLICY`/`AGENT_GUARDRAILS` in config. +50 tests via a `node --test` `.ts` resolver hook. No video upload; keys untouched; no fixed clip count/duration. |
| 2026-06-17 | **Agent timeline ops go through `setHighlights` (not `mergeHighlights`); "add" appends and EXACT ranges are kept verbatim.** | `setHighlights` snapshots for undo and does NOT re-sort, so explicit placement/move order is preserved and a user's precise range is never dropped by the overlap/cap dedupe in `mergeHighlights`. | `lib/timeline/operations.ts` returns full ordered arrays; `runAgentCommand` applies via `setHighlights` + `selectClip`. A later auto plan/merge run still re-groups by source (mergeHighlights sort) — the orchestrator surfaces a note rather than failing silently. Reorder-preserving store is a future item. |
| 2026-06-17 | **OCR is declared as an interface but NOT implemented; the agent answers on-screen-text requests honestly ("OCR isn't ready") and falls back to transcript/visual.** | A capable in-browser OCR is a real bundle/perf cost that needs a size review first; the honesty constraint forbids claiming we read the screen when we didn't. | `lib/ocr/{types,query}.ts` (`OcrEngine` + `queryOnScreenText` → `available:false`; `registerOcrEngine` for a future lazy, capability-gated, local engine). No dependency added. |
| 2026-06-13 | **Add a multi-source `compose` (montage) mode; ship "Option A" (single shared timeline) first, defer "Option B" (true second timeline slot).** Compose builds a FRESH ordered montage from per-source semantic picks across MORE THAN ONE upload, lays it on the existing `highlights` timeline via `setHighlights` (undo-snapshotted), preserves source order unless shuffle/interleave is asked, and labels the run "AI Combined N". | The store has a single `highlights` array and no multi-timeline UI; a real second slot would touch Timeline/render/persistence (large). Option A satisfies "keep uploads immutable" + "don't destroy prior work" (recoverable via undo) without that rework. | New mode end-to-end: `lib/types.ts` (`IntentMode` + `MultiSourceComposePlan` + AgentResponse variant), `app/api/agent/route.ts` (resolveMode + branch + `normalizeComposePlan`), `lib/plan/compose{Normalize,Resolve,Order,Transition,SubPlan}.ts`, `lib/plan/prompt.ts` (`## compose`), `app/editor/page.tsx` (execution branch reusing REAL `executeForSource`). Tests + Node-22 CI job. Option B remains a future timeline-architecture task. |
| 2026-06-13 | **Compose accepts a rich transition vocabulary (glitch/whip/zoom/match_cut) but maps it DOWN to the only renderable transitions (none/fade/crossfade), and says so.** | The ffmpeg render worker only supports cut/fade/crossfade; capturing intent is useful but we must not claim effects we can't render (honesty constraint). | `composeTransition.ts` `mapToRender` + the client summary appends a note when a fancy transition was mapped down. `intended` is kept for UI copy; `Highlight.transition` stores only the renderable value. |
| 2026-06-13 | **Re-introduce WebLLM as an OPTIONAL, opt-in, TEXT-ONLY local planner fallback** (cloud → local → manual), behind `NEXT_PUBLIC_LOCAL_LLM_*` flags (OFF by default) and WebGPU. **Partially revisits** the 2026-06-11 "remove WebLLM" decision: the cloud path stays PRIMARY and unchanged; WebLLM is purely a degraded fallback when the cloud planner fails. | Resilience when the cloud planner is down (overload/quota/network) without re-coupling the whole app to the browser. Kept scoped: text edit-planning only, no vision, lazy-loaded, never on page load, fully on-device (no keys, no upload). | Added `@mlc-ai/web-llm` dep + `lib/local-llm/{config,status,webllm,localPlanner}.ts` + `components/AIModeBadge.tsx`. `app/editor/page.tsx` `handleAgent` adds a tier-2 recovery in the cloud-error branch only. CSP re-allows `raw.githubusercontent.com` (model libs). `/editor` first-load JS unchanged (159 kB) — web-llm is a lazy chunk. Vision asks get a truthful "local vision not enabled" message. Manual editor (upload/trim/render) works with AI fully off. |
| 2026-06-11 | **Support pinning to OpenRouter-only with a single model** (env-driven; documented in `.env.example` as `CLOUD_PROVIDER_ORDER=openrouter` + all `OPENROUTER_*_MODEL=openai/gpt-5.5-pro`). | User wants every route/tier on one model with no Gemini/Groq fallback. | No code change (dispatcher already honors this): `CLOUD_PROVIDER_ORDER=openrouter` → only OpenRouter is tried, failure rethrows (no fallback). Routes use `OPENROUTER_DEFAULT_MODEL`. Key stays server-only; lib/config defaults stay multi-provider so only the deployment's env pins it. Transcription unchanged (local Whisper). |
| 2026-06-11 | **Each idb-keyval store gets its OWN database (one object store per DB) + a self-healing wrapper.** Transcripts moved out of `shorts-studio-cache` into `shorts-studio-transcripts`. `withIdbRecovery` deletes only the affected DB and retries once on a missing-object-store error. | idb-keyval only creates the first object store a DB name ever sees, so sharing one dbName across two stores ("kv" vs "transcripts") crashed with "object store not found"; stale/partial DBs caused the same. | `lib/store/idb.ts` rewritten (public `idbSessions/idbCache/idbLog` shape kept stable); `lib/audio/cache.ts` uses the new `transcripts` DB. Only the affected DB is cleared; no video/key/prompt logging. Persistent failure shows a clean "clear site data" message. |
| 2026-06-11 | **Final clip duration is EXPLICIT-ONLY; no default/forced 30s.** No user-named length → `userSpecifiedDuration=false`, quality-floor selection, emergent total length, and UI/copy say "flexible length". A user-named length ("30s", "make it 15s", "1 minute") → `userSpecifiedDuration=true` + parsed `targetShortSeconds` + budgeted fit. Platform words (TikTok / YouTube Short) imply vertical FORMAT only, never a duration. | A no-duration request should yield a natural reel sized by footage quality, not a forced 30s; only fit to time when the user actually asks. | Prompt D1 rewrite + examples; `PlanPreview`/starter-chip/briefing-chip/activity-log copy de-30s'd; `memoryFromPlan` only persists `memory.duration` when user-set (kills phantom-30s memory leak); `PLAN_DEFAULTS.targetShortSeconds` is a soft non-enforced fallback. Pipeline branch (quality-floor vs budgeted) was already correct. |
| 2026-06-11 | **`CLOUD_PROVIDER_ORDER` env var toggles/re-orders cloud providers** (server-only, comma-separated; e.g. `gemini` = Gemini only, `openrouter` = OpenRouter only). Env overrides the config default; unset → `openrouter,gemini,groq`. | Operators need to switch between OpenRouter and Gemini (or pin one) at deploy time without code edits. | `lib/env.ts` reads it; `lib/providers/cloud.ts` `configuredOrder()` feeds `providerOrder()`. Availability filtering + Groq-text-only + circuit recording unchanged. Not `NEXT_PUBLIC_*`. |
| 2026-06-11 | **Provider circuit breaker is owned by the dispatcher, not the route.** Route-level `checkAllLimits` circuit check is now opt-in (only when a `provider` is passed); the multi-provider dispatcher (`lib/providers/cloud.ts`) skips circuit-open providers and falls through to the next, recording success/failure per provider attempted. | A circuit-open primary (e.g. OpenRouter) must reroute to a healthy fallback (Gemini/Groq), not 503 the request. The old route-level pre-check on `primaryProvider()` blocked that fallback. | `agent`/`briefing`/`clip` drop the `provider` arg; `vision/frame` + `vision/window` (single-provider Gemini-direct, no fallback) keep it for fast-fail. Session + global-budget limits unchanged. If all circuits open, dispatcher still tries (best-effort, self-heals). |
| 2026-06-11 | **Remove the in-browser WebLLM local LLM path; route language/tool decisions SERVER-SIDE via OpenRouter** (OpenAI-compatible), keeping Gemini/Groq as fallbacks. Provider order `OpenRouter → Gemini → Groq` (`CLOUD_PROVIDER_ORDER`). **Supersedes** the 2026-06-08 WebLLM decisions and the 2026-06-11 Hermes high-tier decision. | WebLLM meant multi-GB downloads, WebGPU/device instability, and couldn't be supported universally; a server-side API is simpler and works for everyone. API keys must never run in the browser. | Deleted `lib/llm/*` + `@mlc-ai/web-llm` + `LOCAL_LLM`/`LOCAL_FIRST` + `NEXT_PUBLIC_LOCAL_FIRST_EDITOR`. New `lib/providers/openrouter.ts` + `lib/providers/cloud.ts` + `OPENROUTER` config + env. App is no longer offline/local-LLM. Deterministic client actions (briefing follow-ups, quick shortcuts, promote/extract/reset) remain. |
| 2026-06-11 | **OpenRouter API key is SERVER-ONLY** (`OPENROUTER_API_KEY`); never `NEXT_PUBLIC_*`; vision uses OpenRouter only when the configured model is multimodal, else falls back to direct Gemini (no claim of full Gemini-vision replacement; no fake vision data). | Secrets must never reach the browser bundle; honesty about what actually works. | `serverEnv` only; verified key name absent from `.next/static`. Vision routes gate on `hasGemini() || hasOpenRouter()`. Full video bytes still never leave the browser — only sampled frames. |
| 2026-06-11 | Local-first **high tier** uses **Hermes-3-Llama-3.1-8B** (`q4f16_1-MLC`); Qwen2.5-3B becomes the mid fallback, Llama-3.2-1B stays low. Gemini stays as the cloud planner **and** the vision-briefing fallback. | Hermes-3 is on WebLLM's official `functionCallingModelIds` list (tool-use fine-tuned), so the flag-gated local tool router emits more reliable JSON decisions than a generic instruct model. Vision still needs real frame-tree/caption grounding, so Gemini vision must remain. | `LOCAL_LLM` in `lib/config.ts` only (plus additive `roles` metadata; runtime selectors unchanged). High tier downloads ~4.9 GB / needs more VRAM. No cloud-flow change; flag default still OFF; no fake vision data. Gemini cannot become optional until `lib/frame-tree` + `lib/vision/caption*` + `lib/vision-core` are wired with real data. |
| 2026-06-11 | Briefing follow-ups (and the flag-gated local-first path) dispatch **structured, intent-carrying action objects** (`BriefingFollowUp` / `LocalEditorAction`), not raw text; only deterministic, low-risk actions (`promote`/`extract`/`reset`) run client-side, while scoring/vision-dependent ones (`plan`/`moment`/`describe`/`merge`/`edit`) stay cloud-owned until real frame data exists. | A button click should carry the intent it represents instead of forcing the planner to re-guess from a sentence (the cause of the clarify loop). Executing only deterministic actions locally keeps the timeline safe and avoids faking vision/frame-tree data. | New `BriefingFollowUp` union + `lib/briefing/followups.ts` normalizer; `localFirst.ts` returns `kind:"action"`; editor maps both onto existing store paths. Cloud flow unchanged on flag-off / fall-through. |
| 2026-06-08 | Local-first features (reasoning engine, frame tree, captioning, local LLM) ship as **separate additive library modules** first, wired in later behind a flag. | Keep each piece independently reviewable; never regress the working Gemini flow. | New `lib/vision-core`, `lib/frame-tree`, `lib/vision/caption*`, `lib/llm`; integration is a later, flagged step. |
| 2026-06-08 | Use **WebLLM (`@mlc-ai/web-llm`)** for the local LLM, with a **pre-trained quantized** model (Qwen2.5 / Llama-3.2), not a custom-trained model. | Training in-browser isn't feasible; WebLLM gives offline WebGPU inference with OpenAI-style JSON mode. | Adds ~1GB first-run model download; needs WebGPU; CSP allows `raw.githubusercontent.com` for model libs. |
| 2026-06-08 | Keep a **deterministic engine as the fast path**; the local LLM is a fallback for free-form language; cloud Gemini is the last resort. | "Fast + offline + cheap" conflicts with large local models; determinism stays instant and reliable. | Defines the local-first chain order. |
| (seed) | Browser-first architecture: video never leaves the device; server proxies LLM calls only. | Privacy + $0/low-cost hosting on free tiers. | Shapes the whole pipeline; no video upload endpoints. |

> The bottom "(seed)" row documents a pre-existing foundational decision for
> context. Replace/extend as you learn more about the project's history.
